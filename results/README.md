# Results

Export analysis results for your research:

```bash
curl http://localhost:8082/api/reports/{analysisId}/export > results/my-api.json
```

**Not yet evaluated** — no experimental results collected yet.
